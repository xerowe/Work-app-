<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link href="https://fonts.googleapis.com/css?family=Poppins:300,400,500" rel="stylesheet">
  <link rel="stylesheet" href="/styles/css/styles.css">
  <title>Work Log </title>
</head>
<body>
  <header class="mb-5">
    <nav class="navbar navbar-light bg-light justify-content-between">
      <div class="container-fluid">
      <div class="navbar-header d-flex">
        <a class="navbar-brand pr-5" href="/dashboard">Dantek</a>
        <form class="form-inline">
          <input class="form-control mr-1" type="search" placeholder="Search Dashboard" aria-label="Search">
          <button class="btn btn-outline-light" type="submit"><img src="images/search.svg" width="22" height="22" alt=""></button>
        </form>
      </div>
      <ul class="nav-menu list-unstyled d-flex flex-md-row align-items-md-center">
        <li class="nav-item d-flex align-items-center"><p><%= currentUser.username %></p></li>
        <li class="nav-item"><a class="nav-link btn" href="/logout">Log Out</a></li>
      </ul>
    </div>
    </nav>
  </header>

<!-- UP DONE DONT TOUCH -->
<div class="container-fluid">
  <div class="page-content d-md-flex align-items-stretch">
    <div class="side-navbar">
      <div class="sidebar-header mb-5">
        <div class="avatar"><img src="images/avatar1.svg" alt="avatar" width="64" height="64" class="img-fluid rounded-circle"></div>
        <div class="title">
          <h4><%= currentUser.username %></h4>
          <p class="text-muted">Web Designer</p>
        </div>
      </div>
      <ul class="list-unstyled">
            <li class="active  mb-3 d-flex justify-content-around">
                <img src="images/home1.svg" width="24" height="24" alt="home icon" class="mr-3">
              <a href="/dashboard">Home</a>
            </li>
            <li class="active  mb-3 d-flex justify-content-around">
                <img src="images/product.svg" width="24" height="24" alt="home icon" class="mr-3">
              <a href="/product">Produts</a>
            </li>
            <li class="active  mb-3 d-flex justify-content-around">
                <img src="images/home1.svg" width="24" height="24" alt="home icon" class="mr-3">
              <a href="/dashboard">Home</a>
            </li>
          </ul>
    </div>
    <div class="content-inner">



          <div class="container">
            <% if(error && error.length > 0 ) { %>
              <div class="alert alert-danger" role="alert">
                <%= error %>
              </div>
            <% } %>
            <% if(success && success.length > 0 ) { %>
            <div class="alert alert-success" role="alert">
              <%= success %>
            </div>
            <% } %>
          </div>
